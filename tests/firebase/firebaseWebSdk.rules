rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /firebase-web-sdk/{documentId} {
      allow read, write: if request.auth != null;
    }

    match /firebase-web-sdk-denied/{documentId} {
      allow read, write: if false;
    }
  }
}
